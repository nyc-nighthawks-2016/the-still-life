<h1 class="all-users-header">Sangha</h1>
<ul class="regular-list">
	<% @users.each do |user| %>
	  <li class="each-user">
		  <div class="user-box">
			  <p><%= user.first_name %> <%= user.last_name %><br><%= link_to user.username, user_path(user) %>
			  </p>
		  </div>
	  </li>
	<% end %>
</ul>
